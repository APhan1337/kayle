const conversionFactor = 0.000000001; // {sol} * conversionFactor = # of lamports

function convertLamportToSol(lamports) {
  return null;
}

function convertSolToLamport(sol) {
  return null;
}

module.exports = { convertLamportToSol, convertSolToLamport };
